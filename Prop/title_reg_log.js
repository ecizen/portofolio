import React from 'react'
import { StyleSheet, Text, View } from 'react-native';

export default function title_reg_log({Value}) {
  return (
      <Text style={{marginTop:30, fontSize:32, fontWeight: '600',marginLeft:24}}>{Value}</Text>
  )
}
